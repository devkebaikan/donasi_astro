---
export const prerender = false;

const { slug } = Astro.params;
const search = Astro.url.search;

if (!slug) {
  return Astro.redirect("/donasi");
}

return Astro.redirect(`/${slug}${search}`, 301);
---
